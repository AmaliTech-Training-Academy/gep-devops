{
  "family": "event-planner-booking-service",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",
  "executionRoleArn": "${TASK_EXECUTION_ROLE_ARN}",
  "taskRoleArn": "${TASK_ROLE_ARN}",
  "containerDefinitions": [
    {
      "name": "booking-service",
      "image": "${ECR_REPOSITORY_URL}/booking-service:${IMAGE_TAG}",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 3000,
          "protocol": "tcp",
          "name": "booking-service"
        }
      ],
      "environment": [
        {
          "name": "NODE_ENV",
          "value": "${ENVIRONMENT}"
        },
        {
          "name": "PORT",
          "value": "3000"
        },
        {
          "name": "SERVICE_NAME",
          "value": "booking-service"
        },
        {
          "name": "AWS_REGION",
          "value": "${AWS_REGION}"
        }
      ],
      "secrets": [
        {
          "name": "DATABASE_URL",
          "valueFrom": "${DB_SECRET_ARN}:host::"
        },
        {
          "name": "DATABASE_PASSWORD",
          "valueFrom": "${DB_SECRET_ARN}:password::"
        },
        {
          "name": "DATABASE_USERNAME",
          "valueFrom": "${DB_SECRET_ARN}:username::"
        },
        {
          "name": "DATABASE_NAME",
          "valueFrom": "${DB_SECRET_ARN}:dbname::"
        },
        {
          "name": "REDIS_HOST",
          "valueFrom": "${REDIS_SECRET_ARN}:primary_endpoint::"
        },
        {
          "name": "REDIS_AUTH_TOKEN",
          "valueFrom": "${REDIS_SECRET_ARN}:auth_token::"
        },
        {
          "name": "JWT_SECRET",
          "valueFrom": "${PARAMETER_STORE_PREFIX}/jwt-secret"
        },
        {
          "name": "API_KEY",
          "valueFrom": "${PARAMETER_STORE_PREFIX}/api-key"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/${PROJECT_NAME}-${ENVIRONMENT}",
          "awslogs-region": "${AWS_REGION}",
          "awslogs-stream-prefix": "booking-service",
          "awslogs-create-group": "true"
        }
      },
      "healthCheck": {
        "command": [
          "CMD-SHELL",
          "curl -f http://localhost:3000/api/health || exit 1"
        ],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 60
      },
      "mountPoints": [],
      "volumesFrom": [],
      "dependsOn": [],
      "ulimits": [
        {
          "name": "nofile",
          "softLimit": 65536,
          "hardLimit": 65536
        }
      ],
      "linuxParameters": {
        "initProcessEnabled": true
      },
      "stopTimeout": 120
    }
  ],
  "volumes": [],
  "tags": [
    {
      "key": "Name",
      "value": "${PROJECT_NAME}-${ENVIRONMENT}-booking-service"
    },
    {
      "key": "Environment",
      "value": "${ENVIRONMENT}"
    },
    {
      "key": "Service",
      "value": "booking-service"
    },
    {
      "key": "ManagedBy",
      "value": "Terraform"
    }
  ],
  "runtimePlatform": {
    "cpuArchitecture": "X86_64",
    "operatingSystemFamily": "LINUX"
  }
}
